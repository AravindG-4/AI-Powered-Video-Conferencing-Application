// src/components/Controls.js

import React from 'react';

const Controls = () => {
    return (
        <div>
            <button>Mute</button>
            <button>Leave</button>
        </div>
    );
};

export default Controls;
